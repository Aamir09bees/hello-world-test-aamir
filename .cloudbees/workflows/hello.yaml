apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: Hello World

on:
  push:

jobs:
  hello:
    steps:
      - name: Say Hello
        uses: docker://alpine:latest
        run: echo "Hello World from CloudBees!"
